/**
 * Múltiplos específicos: Crea un programa que muestre todos los múltiplos de 4 que se encuentren entre el 0 y el 40
 */
let numero = 0;
do{
        if (numero % 4 == 0) {
        console.log(numero);
    }

        numero++;

} while(numero<=40);

 