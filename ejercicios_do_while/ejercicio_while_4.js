/**
 * Cuenta regresiva: Realiza un conteo hacia atrás desde el número 15 hasta el 0 utilizando el decremento de la variable de control
 */

let conteo = 15;
do{
    console.log("faltan: " + conteo)

conteo--;
}while(conteo >=0);